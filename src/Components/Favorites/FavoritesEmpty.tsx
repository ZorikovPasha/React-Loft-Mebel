import React from "react";

const FavoritesEmpty: React.FC = () => {
  return (
    <div className="favorites__empty">
      <p className="favorites__empty-text">Вам ничего не понравилось(</p>
    </div>
  );
};

export default FavoritesEmpty;
